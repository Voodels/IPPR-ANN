 Channel')
plt.imshow(b, cmap='Blues')
plt.axis('off')
plt.tight_layout()
plt.show()
```
- **Why?**
  - Sets up a 2x2 grid for displaying images.
  - Shows the original image and each RGB channel with appropriate color maps.
  - `tight_layout()` ensures proper spacing, and `show()` renders the figure.

---

### 📊 Displaying CMY Channels

```python
plt.figure(figsize=(12, 8))
plt.subplot(2, 2, 1)
plt.title('Original Image')
plt.imshow(img)
plt.axis('off')

plt.subplot(2, 2, 2)
plt.title('Cyan Channel')
plt.imshow(c, cmap='winter')
plt.axis('off')

plt.subplot(2, 2, 3)
plt.title('Magenta Channel')
plt.imshow(m, cmap='magma')
plt.axis('off')

plt.subplot(2, 2, 4)
plt.title('Yellow Channel')
plt.imshow(y, cmap='YlOrBr')
plt.axis('off')
plt.tight_layout()
plt.show()
```
- **Why?**
  - Again, shows the original image for comparison.
  - Displays each CMY channel with visually distinct color maps.

---

## ✅ Summary Table

| Step                | Purpose                                      | Function/Concept         |
|---------------------|----------------------------------------------|-------------------------|
| Import Libraries    | Image processing & visualization             | `cv2`, `numpy`, `matplotlib` |
| Read Image          | Load image into program                      | `cv2.imread`            |
| Convert Color Space | Ensure correct color display                 | `cv2.cvtColor`          |
| Split Channels      | Analyze individual color components          | `cv2.split`             |
| Compute CMY         | Get printing color model channels            | `255 - channel`         |
| Display Images      | Visualize results for understanding          | `matplotlib` subplots   |

---

## 🌟 Key Takeaways

- **Channel splitting** helps in understanding how colors combine in images.
- **CMY model** is crucial for printing and complements the RGB model.
- **Visualization** is essential for verifying image processing steps.